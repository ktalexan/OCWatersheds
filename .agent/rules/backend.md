---
trigger: glob
globs: **/*.{cs,java}
---

# Backend OOP Standards
- **C# / .NET:** Use `PascalCase` for methods/classes, `camelCase` for private fields (with `_` prefix).
- **Java:** Use `camelCase` for methods, `PascalCase` for classes, and `camelCase` for constants.
